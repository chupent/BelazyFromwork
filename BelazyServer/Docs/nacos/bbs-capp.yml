# 端口配置
server:
  port: 8211

# ADMIN检测地址
spring:
  boot:
    admin:
      client:
        url: http://localhost:8080
        
# 对外暴露接口        
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS