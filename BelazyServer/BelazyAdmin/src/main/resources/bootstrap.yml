spring:
  application:
    name: belazy-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}
  profiles:
    active: dev
server:
  port: 8080